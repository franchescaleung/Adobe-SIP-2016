#attr: size, color, weight, trunk size, feet size
#methods: drink, run, move, walk

class Elephant():
    def __init__(self, size, color, weight, trunksize, feetsize,name):
        self.size=size
        self.color=color
        self.weight=weight
        self.trunksize=trunksize
        self.feetsize=feetsize
        self.name=name

    #methods
    def drink(drink):
        print(self.name+ " drinks" drink+"!")

    def run(length):
        print(self.name + " runs " + length + " feet!")

Michael= Elephant()















    